// types.ts (or you can define it directly in the component file)
export interface Course {
  id: number;
  name: string;
  beschreibung: string;
  startDatum: string;
  endDatum: string;
  preis: number;
  dauer: string;
  bild: string;
}
